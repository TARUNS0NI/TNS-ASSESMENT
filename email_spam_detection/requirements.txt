# ------------------------------------------------------------------
# DEPENDENCY MANIFEST: requirements.txt
# Install everything with:  python -m pip install -r requirements.txt
# ------------------------------------------------------------------

# Modern, high-performance web framework used to build the prediction API
fastapi

# ASGI web server that actually runs and serves the FastAPI application
uvicorn

# Core machine learning library: TF-IDF vectorizer + Naive Bayes classifier + metrics
scikit-learn

# Data analysis library used to read spam_emails.csv into a DataFrame
pandas

# Fundamental scientific computing package (used internally by scikit-learn)
numpy

# Serialization utility used to save (.pkl) and load the trained model and vectorizer
joblib

# Data validation library used by FastAPI to enforce a strict request schema
pydantic
